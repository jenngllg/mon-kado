# Local defaults. Replace API_HOST with the real API subdomain on the VPS.
API_HOST=http://localhost
HTTP_PORT=8080
HTTPS_PORT=8443

POSTGRES_DB=mon_kado
POSTGRES_USER=mon_kado
POSTGRES_PASSWORD=replace-with-a-long-hexadecimal-password

IMAGE_TAG=local
