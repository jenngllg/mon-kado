[project]
name = "monkado-local-monitoring"
version = "1.0.0"
requires-python = ">=3.12"
description = "Bounded local observations and explicit incident notifications, without application dependencies"
